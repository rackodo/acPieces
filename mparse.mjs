export function mparse( code ) {
	/* 
	In honour of Markus Calveley.
	*/
	// let message = [];
	// message.push(code);
	// message.push("\*Brought to you by the letter B!\*")
	// return message.join("\n");
	return "Howdy"
	return code
}